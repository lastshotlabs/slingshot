---
title: 'Legacy: Config-Driven Overview'
description: Legacy name for the package-first and entity-system authoring path
---

import { CardGrid, LinkCard } from '@astrojs/starlight/components';

`Config-driven` is the older name for what the docs now split into two clearer sections:

- [Package-First Authoring](/slingshot/package-first/)
- [Entity System](/slingshot/entity-system/)

Those sections are now the canonical docs path.

## If you landed here

Use this mapping:

- `definePackage(...)`, `domain(...)`, `route.*(...)`, capabilities, and `entityRef(...)`
  now live under [Package-First Authoring](/slingshot/package-first/)
- `defineEntity(...)`, `defineOperations(...)`, storage wiring, generated routes, overrides, and
  extra routes now live under [Entity System](/slingshot/entity-system/)

## Canonical path

<CardGrid>
  <LinkCard
    title="Quick Start"
    href="/slingshot/quick-start/"
    description="Start with the normal app scaffold."
  />
  <LinkCard
    title="App Authoring"
    href="/slingshot/app-authoring/"
    description="Learn the core API surface: app assembly, config, context, events, WebSockets, SSE, and composition."
  />
  <LinkCard
    title="Package-First Authoring"
    href="/slingshot/package-first/"
    description="Learn the canonical package-level authoring model."
  />
  <LinkCard
    title="Entity System"
    href="/slingshot/entity-system/"
    description="Learn the canonical model for data-shaped domains."
  />
</CardGrid>

## Why this changed

`Config-driven` was too broad. It mixed:

- package authoring
- entity authoring
- route policy
- storage wiring
- escape hatches

That made it harder for users to answer basic questions like:

- how do I scaffold an app?
- where is the normal package story?
- where is the normal entity story?
- where do the advanced customizations begin?

The new split makes those paths explicit.

## Legacy deep links

The old `config-driven/*` pages still exist for compatibility and deeper reference material, but
they are no longer the main learning path.
