---
title: Entity System
description: Define a data model, get a full API — then add operations, auth, permissions, and storage
---

import { CardGrid, LinkCard } from '@astrojs/starlight/components';

If you followed the [Quick Start](/slingshot/quick-start/) or
[Data and Entities](/slingshot/core-features/data-and-entities/), you already have an entity
generating routes. This section goes deeper into everything the entity system can do.

## What you will learn

- How to define fields, indexes, timestamps, and relations
- How to add named operations like lookups, transitions, and search
- How to control auth, permissions, and rate limits per generated route
- How to swap storage backends without changing your entity code
- How to override or extend generated routes when the default behavior is not enough

## The progression

Most entities start simple and grow. Here is what that looks like:

### 1. Start with a model

```typescript
import { defineEntity, field } from '@lastshotlabs/slingshot';

const Post = defineEntity('Post', {
  namespace: 'blog',
  fields: {
    id: field.string({ primary: true, default: 'uuid' }),
    title: field.string(),
    body: field.string(),
    createdAt: field.date({ default: 'now' }),
  },
});
```

You now have `GET /posts`, `POST /posts`, `PATCH /posts/:id`, `DELETE /posts/:id` with
validation and OpenAPI docs.

### 2. Add domain behavior

Your blog needs "publish" and "lookup by author." These are operations:

```typescript
import { defineOperations, op } from '@lastshotlabs/slingshot';

const PostOps = defineOperations(Post, {
  byAuthor: op.lookup({
    fields: { authorId: 'param:authorId' },
    returns: 'many',
  }),
  publish: op.transition({
    field: 'status',
    from: 'draft',
    to: 'published',
    match: { id: 'param:id' },
  }),
});
```

Now you also have `GET /posts/by-author/:authorId` and `POST /posts/:id/publish`.

### 3. Add auth and permissions

Readers can browse. Only authenticated users can create. Only the author can edit:

```typescript
const Post = defineEntity('Post', {
  namespace: 'blog',
  fields: {
    id: field.string({ primary: true, default: 'uuid' }),
    title: field.string(),
    body: field.string(),
    authorId: field.string(),
    status: field.enum(['draft', 'published'] as const, { default: 'draft' }),
    createdAt: field.date({ default: 'now' }),
  },
  routes: {
    defaults: { auth: 'userAuth' },
    list: { auth: 'none' },
    get: { auth: 'none' },
    update: {
      permission: { requires: 'blog:post.write', ownerField: 'authorId' },
    },
  },
});
```

### 4. Swap storage

Move from in-memory to a real database with one change:

```typescript
entity({ config: Post, operations: PostOps, adapter: 'postgres' });
```

Your entity definition, operations, and route policy stay the same.

## Read in order

<CardGrid>
  <LinkCard
    title="defineEntity"
    href="/slingshot/entity-system/define-entity/"
    description="Fields, indexes, timestamps, relations, system fields, and naming conventions."
  />
  <LinkCard
    title="Operations"
    href="/slingshot/entity-system/operations/"
    description="Lookups, transitions, field updates, search, aggregates, and batches."
  />
  <LinkCard
    title="Route Policy"
    href="/slingshot/entity-system/route-policy/"
    description="Auth, permissions, middleware, rate limits, and event publication per generated route."
  />
  <LinkCard
    title="Storage and Adapter Wiring"
    href="/slingshot/entity-system/storage-and-adapter-wiring/"
    description="Memory, SQLite, Postgres, MongoDB, Redis — and how to swap between them."
  />
  <LinkCard
    title="Generated Routes, Overrides, and Extra Routes"
    href="/slingshot/entity-system/generated-routes-overrides-and-extra-routes/"
    description="Customize one generated route, add your own, or break out of the entity shell."
  />
</CardGrid>

## Common patterns

| You want to...                                  | See                                                                                      |
| ----------------------------------------------- | ---------------------------------------------------------------------------------------- |
| Look up records by a field                      | [Operations: lookups](/slingshot/entity-system/operations/)                              |
| Add a status transition (draft → published)     | [Operations: transitions](/slingshot/entity-system/operations/)                          |
| Require login on some routes but not others     | [Route Policy](/slingshot/entity-system/route-policy/)                                   |
| Let users only edit their own records           | [Route Policy: ownerField](/slingshot/entity-system/route-policy/)                       |
| Use Postgres instead of in-memory               | [Storage and Adapter Wiring](/slingshot/entity-system/storage-and-adapter-wiring/)       |
| Override a single generated route               | [Route overrides](/slingshot/entity-system/generated-routes-overrides-and-extra-routes/) |
| Add a route that is not generated by the entity | [Extra routes](/slingshot/entity-system/generated-routes-overrides-and-extra-routes/)    |
| Drop out of the entity system entirely          | [Escape Hatches](/slingshot/package-first/escape-hatches/)                               |

## After this section

- [Examples](/slingshot/examples/) — complete apps that use entities with auth, events, and more
- [Guides](/slingshot/guides/) — production concerns like testing, deployment, and scaling
