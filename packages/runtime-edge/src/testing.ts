// ---------------------------------------------------------------------------
// @lastshotlabs/slingshot-runtime-edge/testing — Test utilities
// ---------------------------------------------------------------------------

/** Standard timeout for edge runtime operations in tests. */
export const TEST_EDGE_TIMEOUT_MS = 5_000;

/** Maximum file bytes for edge runtime file reads in tests. */
export const TEST_MAX_FILE_BYTES = 1024 * 1024; // 1 MB
