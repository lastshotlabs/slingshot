/**
 * Runtime filter expression evaluator — re-exported from slingshot-core.
 *
 * slingshot-core is the canonical location. This file re-exports for
 * backwards compatibility with existing configDriven imports.
 */
export {
  evaluateFilter,
  extractFilterParams,
  extractMatchParams,
  resolveMatch,
} from '@lastshotlabs/slingshot-core';
